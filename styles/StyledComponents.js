import { Box, styled } from "@mui/material";

const HeaderBox = styled(Box)({
  height: "800px",
  background:
    "transparent linear-gradient(5deg, rgba(110, 142, 249, 0.2) 0%, rgba(135, 241, 241, 0.2) 100%) 0% 0% no-repeat padding-box;",
});

export default HeaderBox;
